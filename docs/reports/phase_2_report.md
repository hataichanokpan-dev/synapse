# Phase 2 Report: Fork Graphiti

**Date:** 2026-03-13
**Author:** Fon (Claude)
**Session:** Phase 2 Implementation

---

## Summary

Phase 2 เสร็จสมบูรณ์พร้อม Critical bug fixes จาก Codex review

---

## Tasks Completed

| Task | Status | Notes |
|------|--------|-------|
| 2.1 Clone Graphiti | ✅ Done | Cloned to `temp_graphiti/` then copied |
| 2.2 Copy core modules | ✅ Done | `synapse/graphiti/` + `synapse/mcp_server/` |
| 2.3 Docker setup | ✅ Done | `docker-compose.yml` + `Dockerfile` |
| 2.4 Fix Critical Issues | ✅ Done | 2 Critical + 2 High issues fixed |

---

## Bug Fixes (From Codex Review)

### Critical Issues Fixed

| # | Issue | Fix |
|---|-------|-----|
| 1 | Entrypoint `synapse-mcp` ชี้ไป `main:main` แต่ไม่มี function | เพิ่ม `main()` function ใน `synapse/mcp_server/main.py` |
| 2 | Dockerfile ไม่มี dependencies ที่จำเป็น | เพิ่ม `pydantic-settings`, `pyyaml`, `starlette`, `typing-extensions` ใน root `pyproject.toml` |

### High Issues Fixed

| # | Issue | Fix |
|---|-------|-----|
| 1 | `temp_graphiti/` folder ยังไม่ได้ลบ | `rm -rf temp_graphiti/` |
| 2 | Port mismatch (8000 vs 47780) | แก้ `config.yaml` ให้ใช้ port 47780 |
| 3 | FALKORDB_URI format ผิด | เปลี่ยนจาก `falkordb://` เป็น `redis://` |

### Medium/Low Issues Addressed

| # | Issue | Fix |
|---|-------|-----|
| 1 | ChromaDB auth token env var | เพิ่ม `CHROMA_AUTH_TOKEN` env var |
| 2 | Vendored code ไม่มี docstring | เพิ่ม docstring ใน `synapse/graphiti/__init__.py` |

---

## Files Modified

### New Files
- `synapse/graphiti/` - Vendored Graphiti core (21 files)
- `synapse/mcp_server/` - Vendored MCP server (20+ files)
- `docker-compose.yml` - Full stack setup
- `Dockerfile` - Container for Synapse MCP

### Modified Files
- `pyproject.toml` - Added all required dependencies
- `synapse/mcp_server/main.py` - Added `main()` function
- `synapse/mcp_server/config/config.yaml` - Changed port to 47780
- `synapse/graphiti/__init__.py` - Added docstring

### Deleted
- `temp_graphiti/` - Cleaned up after copying

---

## Current Project Structure

```
synapse/
├── synapse/
│   ├── __init__.py
│   ├── layers/
│   │   ├── types.py      # Five-Layer types
│   │   └── decay.py      # Decay scoring
│   ├── graphiti/         # Vendored Graphiti core
│   ├── mcp_server/       # Vendored MCP server
│   ├── nlp/              # (empty, for Phase 4)
│   ├── storage/          # (empty, for Phase 3)
│   └── mcp/              # (empty, for Phase 5)
├── docs/
├── tests/
├── docker-compose.yml
├── Dockerfile
├── pyproject.toml
└── README.md
```

---

## Dependencies Added

```toml
# New dependencies in pyproject.toml
"graphiti-core>=0.28.2",
"pydantic-settings>=2.0.0",
"pyyaml>=6.0",
"starlette>=0.36.0",
"typing-extensions>=4.0.0",
```

---

## Verification Steps

```bash
# 1. Install dependencies
pip install -e ".[dev]"

# 2. Check imports work
python -c "from synapse.mcp_server.main import main; print('OK')"

# 3. Start services
docker compose up -d falkordb chromadb

# 4. Test MCP server
synapse-mcp --help
```

---

## Next Steps (Phase 3)

1. **Task 3.1:** Implement Layer 1 (User Model)
2. **Task 3.2:** Implement Layer 2 (Procedural)
3. **Task 3.3:** Implement Layer 3 (Semantic) - mostly in Graphiti
4. **Task 3.4:** Implement Layer 4 (Episodic)
5. **Task 3.5:** Implement Layer 5 (Working)

---

## Risks & Mitigations

| Risk | Status | Mitigation |
|------|--------|------------|
| Vendored code not used | ⚠️ Pending | Phase 3 will integrate or remove |
| Port conflicts | ✅ Fixed | Using 47780 consistently |
| Missing dependencies | ✅ Fixed | Added all to root pyproject.toml |

---

*Report generated by Fon on 2026-03-13*
