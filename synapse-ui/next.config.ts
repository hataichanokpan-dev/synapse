import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No rewrites - use Next.js API routes directly
  // API routes are in app/api/* and handled by Next.js
};

export default nextConfig;
